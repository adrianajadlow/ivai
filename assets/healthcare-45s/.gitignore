# IVAI Healthcare 45s — build artifacts (regenerated, not version-controlled)

# Node / render engine
node_modules/
package-lock.json

# Intermediate frames + working output
frames/
out/
*.mp4

# Third-party fonts (fetched by scripts)
fonts/

# Source VO + reference copies (provided separately)
narration.mp3
ref_endslate_vertical.png

# Client photos (extracted to disk for compositing; not version-controlled)
broll/
